{
  "name": "@soroban-lens/web",
  "version": "0.1.0",
  "description": "Module 4 — single page Soroban event explorer.",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "typecheck": "tsc -b --force",
    "preview": "vite preview --host 0.0.0.0",
    "test": "npm run build && tsc -p tsconfig.test.json && node --test test/*.test.js"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "^7.0.2",
    "vite": "^7.1.0"
    "typescript": "^5.9.0",
    "vite": "^8.3.0"
  }
}
