global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - ingestion-alerts.yml

scrape_configs:
  - job_name: soroban-lens-ingest
    metrics_path: /metrics
    static_configs:
      - targets: ['127.0.0.1:9090']
